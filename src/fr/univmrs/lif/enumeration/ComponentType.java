package fr.univmrs.lif.enumeration;

public enum ComponentType {
	
	AND, NAND, OR, NOR, NOT, XOR, XNOR,
	JOINT,
	MODULE, INPUTMODULE, OUTPUTMODULE,
	WIRE,
	PLUG, 
	INPUT, SWITCH, CLOCK,
	OUTPUT, LED, DISPLAY,
	MUX, DECODER, ADDER, SENSOR, VDD, GND
}
